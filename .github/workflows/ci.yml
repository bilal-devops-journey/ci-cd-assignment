name: Continuous Integration

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-late

    steps:
      - uses: actions/checkout@v4

      - name: Test CI
        run: echo "CI is working!"